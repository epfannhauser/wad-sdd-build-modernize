Look at the source file I provide. Write unit tests that cover the main exported functions.

Requirements:

- Place tests next to the source as `<filename>.test.ts`.
- Use `node:test` and `node:assert/strict`.
- Cover normal behavior and validation/error cases.
- Keep the example dependency-free.
- Run the test with `node --test <filename>.test.ts`.

Additional context or target file:

$ARGUMENTS
